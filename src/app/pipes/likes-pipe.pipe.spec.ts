import { LikesPipePipe } from './likes-pipe.pipe';

describe('LikesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LikesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
